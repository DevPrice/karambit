#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# eslint can't parse this branch: typescript-eslint reads the compiler API, which TypeScript 7
# doesn't ship. Restore `npx lint-staged` once the linter is replaced.
echo "pre-commit lint skipped (see .husky/pre-commit)"
